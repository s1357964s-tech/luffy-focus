{
  "name": "luffy-focus-functions",
  "description": "Firebase Functions for Luffy Focus pet generation",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "lint": "node --check index.js",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^6.1.2"
  }
}
